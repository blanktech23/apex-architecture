export const agents = [
  {
    id: 'leads-agent',
    name: 'Leads Agent',
    subtitle: 'Lead Qualification & Booking',
    color: '#3b82f6',
    icon: 'Search',
    description: 'Captures and qualifies every lead automatically using the built-in CRM',
    details: [
      'Responds to new leads within 15 minutes',
      'Qualifies prospects against your ideal client profile',
      'Schedules consultations and sends prep materials',
      'Tracks every lead from first contact to signed agreement',
    ],
    integrations: ['crm', 'email', 'calendar'],
    humanApproval: [
      'Sending any communication beyond initial acknowledgment',
      'Declining a lead or sending referral',
      'Nurture email sequences',
    ],
    metrics: [
      'Lead response time',
      'Qualification rate',
      'Consultation conversion',
      'Pipeline value',
    ],
    infrastructure: {
      execution: 'Node.js TypeScript process on a shared DigitalOcean VPS (8 vCPU / 16GB RAM) running Docker Compose. One server runs all customer agents — not a separate instance per customer. Tenant context is injected at the request boundary and never trusted from the client.',
      ai: 'Inbound lead emails are classified by GPT-4.1 Nano ($0.10/1M input tokens) for lead scoring. Email draft generation routes to Claude Haiku 4.5 ($1/1M input). Complex qualification analysis escalates to Claude Sonnet 4.6 ($3/1M input). Model selection is automatic via Vercel AI Gateway\'s per-tenant API keys with spending caps + fallback routing.',
      events: 'Subscribes to NATS JetStream subject TENANT_{id}.webhook.email.inbound — when SendGrid parses a lead email, it POSTs to the VPS at /webhooks/sendgrid/:tenantId, which validates the signature, logs the raw event to the event_bus_log table, and publishes to NATS. The Leads Agent picks it up within seconds.',
      scheduling: 'Follow-up sequences and nurture cadences are BullMQ repeatable jobs stored in Valkey. Each job carries the tenant_id and retry logic (3 attempts with exponential backoff). The job queue also handles the 15-minute SLA: if no response is drafted within 15 min, an escalation is created.',
      data: 'Lead records, qualification scores, and pipeline stages stored in the Supabase PostgreSQL database. Row-Level Security policy enforces tenant_id = auth.jwt() ->> \'tenant_id\' on every query. Draft emails sit in the drafts table until human approval. All agent runs logged to agent_executions with input/output and token cost.',
      hosting: 'Single multi-tenant VPS handles all customers. Tenant isolation: NATS subject namespacing (TENANT_{id}.agent.leads-agent.*), Supabase RLS on every table, per-tenant Vercel AI Gateway API key with spending limits. Adding customer #101 costs ~$1.50/mo incremental — no new infrastructure needed.',
    },
  },
  {
    id: 'design-agent',
    name: 'Design Agent',
    subtitle: 'Design Specs & Material Selection',
    color: '#a855f7',
    icon: 'Palette',
    description: 'Tracks every selection, purchase order, and specification',
    details: [
      'Maintains a live register of every material selection',
      'Tracks client approvals and flags delays',
      'Drafts purchase orders for supplier review',
      'Compiles complete handoff packages for the build team',
    ],
    integrations: ['jobtread', 'drive', 'email'],
    humanApproval: [
      'Sending any purchase order',
      'Communicating with clients about selections',
      'Marking design phase complete',
    ],
    metrics: [
      'Selection completion %',
      'Approval turnaround time',
      'Budget vs allowance',
      'Outstanding items',
    ],
    infrastructure: {
      execution: 'Node.js TypeScript process on the shared DigitalOcean VPS running Docker Compose. Implements the AgentPlugin interface — a self-contained module with defined triggers, permissions, and an execute() method. Tenant context injected at request boundary.',
      ai: 'Document formatting and selection tracking use Claude Haiku 4.5 ($1/1M input tokens) for speed. Purchase order analysis and handoff package compilation route to Claude Sonnet 4.6 ($3/1M input) for reasoning quality. Vercel AI Gateway handles model selection per request — system prompts are cached for 90% input token discount on repeat calls.',
      events: 'Subscribes to NATS JetStream subjects: TENANT_{id}.agent.design-agent.* for direct triggers and TENANT_{id}.webhook.jobtread.* for JobTread updates. When a client submits a material selection or approves a spec, the event flows through NATS to this agent for processing.',
      scheduling: 'BullMQ delayed jobs handle approval reminder follow-ups — if a selection hasn\'t been approved within the configured window, a reminder is queued. PO draft deadlines are tracked as repeatable jobs with tenant-scoped queues.',
      data: 'Selection registers stored as JSONB in the agents table (flexible schema without migrations). PO drafts sit in the drafts table pending human approval. Files organized in Google Drive: Shared Drive → Project → Room → Category (Submittals, Cut Sheets, Warranties). All queries filtered by Supabase RLS (tenant_id = auth.jwt() ->> \'tenant_id\').',
      hosting: 'Single multi-tenant VPS — no per-customer servers. Isolation via NATS subject namespacing, Supabase RLS, and per-tenant Vercel AI Gateway API keys with spending caps. Agent config stored in agent_templates table — the "Construction Remodeling" template defines this agent\'s triggers and permissions.',
    },
  },
  {
    id: 'project-management-agent',
    name: 'Project Management Agent',
    subtitle: 'Schedule & Budget Tracking',
    color: '#f97316',
    icon: 'BarChart3',
    description: 'Monitors schedules, budgets, and subcontractors in real-time',
    details: [
      'Tracks schedule drift and flags delays early',
      'Monitors budget vs actual spending per line item',
      'Coordinates subcontractor arrivals and inspections',
      'Generates daily job site briefings for project managers',
    ],
    integrations: ['jobtread', 'weather', 'quickbooks'],
    humanApproval: [
      'Modifying any schedule date',
      'Approving change orders',
      'Communicating with clients about project status',
    ],
    metrics: [
      'Schedule variance',
      'Budget burn rate',
      'Open RFIs',
      'Punch list items',
    ],
    infrastructure: {
      execution: 'Node.js TypeScript process on the shared DigitalOcean VPS (Docker Compose). This is the most event-driven agent — it reacts to webhook data from JobTread, QuickBooks, and OpenWeatherMap in near real-time. Tenant context injected at request boundary, never from client.',
      ai: 'Daily status checks and schedule comparisons use GPT-4.1 Nano ($0.10/1M input) for speed. Schedule risk analysis and drift forecasting route to Claude Sonnet 4.6 ($3/1M input). Weather impact assessments use Claude Haiku 4.5 ($1/1M input). Each request has a max_tokens cap (4,096 standard) to prevent runaway costs.',
      events: 'Subscribes to three NATS JetStream subjects: TENANT_{id}.webhook.jobtread.schedule_change for schedule updates, TENANT_{id}.webhook.quickbooks.payment for budget actuals, and TENANT_{id}.webhook.weather.alert for severe weather. JobTread webhooks hit /webhooks/jobtread/:tenantId on the VPS, validated and routed through NATS.',
      scheduling: 'Daily job site briefings generated by a BullMQ repeatable cron job — fires each morning per tenant\'s configured time. Weather data polled via BullMQ scheduled jobs for each active job site (OpenWeatherMap returns UTC — converted to local timezone per site). Loop detection aborts if same tool called 3x consecutively.',
      data: 'Schedule snapshots, budget burn tracking, and sub coordination logs stored in Supabase PostgreSQL. Agent outputs logged to agent_executions table with full input/output and token cost. Escalations (schedule drift > threshold) written to the escalations table for human review. All filtered by RLS policy.',
      hosting: 'Single multi-tenant VPS — all customers share one server. Isolation: NATS subjects scoped to TENANT_{id}.agent.project-management-agent.*, Supabase RLS on every table, per-tenant Vercel AI Gateway key with daily spend cap at 2x expected usage.',
    },
  },
  {
    id: 'sales-agent',
    name: 'Sales Agent',
    subtitle: 'Estimates & Proposals',
    color: '#22c55e',
    icon: 'Calculator',
    description: 'Builds accurate estimates from historical project data',
    details: [
      'Generates rough estimates with confidence ranges (not single numbers)',
      'Learns from every completed project to improve accuracy',
      'Manages subcontractor bid comparison and analysis',
      'Tracks contingency usage and margin in real-time',
    ],
    integrations: ['jobtread', 'quickbooks'],
    humanApproval: [
      'Presenting any estimate to a client',
      'Selecting a subcontractor',
      'Applying markup different from approved formula',
    ],
    metrics: [
      'Estimate accuracy',
      'Win rate',
      'Gross margin',
      'Contingency remaining',
    ],
    infrastructure: {
      execution: 'Node.js TypeScript process on the shared DigitalOcean VPS (Docker Compose). The most compute-intensive agent — estimate generation involves multi-step reasoning across historical data. Triggered via API request: User clicks "Run Estimate" → REST API → BullMQ job → Agent executes → result stored in Supabase → NATS event → WebSocket push to dashboard.',
      ai: 'Cost analysis and bid comparison require Claude Sonnet 4.6 ($3/1M input, $15/1M output) — the highest-tier model used routinely. Historical data lookups use Claude Haiku 4.5. Prompt caching saves 90% on system prompt tokens (the estimate template is reused across runs). Per-agent circuit breaker: if a single execution exceeds $5 in token cost, it aborts and alerts.',
      events: 'Subscribes to NATS JetStream subject TENANT_{id}.agent.sales-agent.request for user-triggered estimates. Also listens to TENANT_{id}.webhook.jobtread.bid_received for incoming subcontractor bids. Results published back to NATS for the CEO Agent to aggregate.',
      scheduling: 'Estimate jobs queued via BullMQ with progress tracking — long-running calculations report percentage complete to the dashboard via WebSocket. Timeout protection: jobs exceeding 5 minutes are killed and escalated. Retry logic: 3 attempts with exponential backoff on transient failures (API timeouts, rate limits).',
      data: 'Historical cost data pulled from completed JobTread projects and QuickBooks actuals. Bid comparisons, estimate versions, and confidence ranges stored as JSONB in agent_executions. Each run logs input tokens, output tokens, model used, and dollar cost. QuickBooks data accessed read-only — the app layer wraps the QB API in a read-only adapter that blocks all write methods.',
      hosting: 'Single multi-tenant VPS — no per-customer servers. Per-tenant Vercel AI Gateway API key with monthly spending limit. Daily usage synced to Supabase for the billing dashboard. At ~4,500 customers, plan to negotiate direct Anthropic/OpenAI volume discounts for 15-20% savings over Vercel AI Gateway.',
    },
  },
  {
    id: 'bookkeeping-agent',
    name: 'Bookkeeping Agent',
    subtitle: 'AP/AR & Financial Operations',
    color: '#14b8a6',
    icon: 'Shield',
    description: 'Manages billing, compliance, and cash flow visibility',
    details: [
      'Monitors accounts receivable aging and drafts follow-up notices',
      'Tracks insurance and license compliance for all subcontractors',
      'Compiles monthly WIP schedules and P&L summaries',
      'Flags billing milestones and prepares payment checklists',
    ],
    integrations: ['quickbooks', 'drive'],
    humanApproval: [
      'Issuing any invoice or making any payment',
      'Filing any tax or government document',
      'Negotiating payment terms with vendors',
    ],
    metrics: [
      'AR aging',
      'Cash flow projection',
      'Insurance compliance',
      'WIP position',
    ],
    infrastructure: {
      execution: 'Node.js TypeScript process on the shared DigitalOcean VPS (Docker Compose). Implements the AgentPlugin interface with read-only QuickBooks access enforced at the app layer — the QB REST API technically allows writes, but the Bookkeeping Agent\'s IntegrationAdapter wraps it in a read-only interface that blocks all write methods.',
      ai: 'Invoice formatting and AR aging summaries use Claude Haiku 4.5 ($1/1M input) for speed. P&L compilation and cash flow analysis route to Claude Sonnet 4.6 ($3/1M input) for financial reasoning accuracy. System prompts cached via Vercel AI Gateway — 90% discount on repeat input tokens across invocations.',
      events: 'Subscribes to NATS JetStream subjects: TENANT_{id}.webhook.quickbooks.invoice for payment events and TENANT_{id}.webhook.quickbooks.payment for cash receipts. Also listens to TENANT_{id}.agent.bookkeeping.compliance for insurance/license expiry triggers. Billing milestone events published by the Project Management Agent arrive here for invoice preparation.',
      scheduling: 'Monthly WIP schedules and P&L summaries generated as BullMQ repeatable cron jobs. AR aging checks run daily — if invoices exceed configured aging thresholds (30/60/90 days), collection notice drafts are created in the drafts table for human approval. Insurance compliance checks fire weekly per subcontractor.',
      data: 'Financial records synced from QuickBooks (read-only via OAuth 2.0, access_token refreshed hourly, refresh_token expires after 100 days of non-use). Compliance documents and insurance certificates stored in Google Drive (Shared Drive → Project → Compliance). Invoice drafts and lien waivers in Supabase drafts table. All queries filtered by RLS: tenant_id = auth.jwt() ->> \'tenant_id\'.',
      hosting: 'Single multi-tenant VPS — all customers share infrastructure. QuickBooks rate limit: 100 requests per 10 seconds per tenant. Per-tenant Vercel AI Gateway API key with daily spend cap. The app layer enforces QB read-only — this is a deliberate architectural decision, not a QuickBooks permission setting.',
    },
  },
  {
    id: 'ceo-agent',
    name: 'CEO Agent',
    subtitle: 'Executive Briefings & Analytics',
    color: '#eab308',
    icon: 'Crown',
    description: 'Your private intelligence layer for fast, informed decisions',
    details: [
      'Delivers a 3-minute morning briefing every day at 7 AM',
      'Tracks portfolio health across all active projects',
      'Maintains rolling 12-month revenue forecast',
      'Manages escalation queue \u2014 nothing falls through the cracks',
    ],
    integrations: ['jobtread', 'quickbooks'],
    humanApproval: [
      'All outputs are for CEO eyes only',
      'Never makes strategic recommendations \u2014 only surfaces data',
      'Never takes action on escalations \u2014 organizes them for your decision',
    ],
    metrics: [
      'Portfolio health',
      'Pipeline forecast',
      'Gross margin trend',
      'Escalation age',
    ],
    infrastructure: {
      execution: 'Node.js TypeScript process on the shared DigitalOcean VPS (Docker Compose). The only agent that reads across all other agents\' outputs — it subscribes to every agent\'s NATS subjects to aggregate data for the CEO. Flow: BullMQ cron fires → agent pulls from all 6 agent outputs → generates briefing draft → stores in drafts table → WebSocket notification pushes to CEO dashboard.',
      ai: 'Morning briefings and strategic analyses use Claude Sonnet 4.6 ($3/1M input, $15/1M output) — Opus ($75/1M output) is unnecessary for any production use case, even CEO briefings perform well on Sonnet 4.6 at 40% savings with negligible quality difference in structured business contexts. Prompt caching critical here: the briefing system prompt (which includes the full agent network context) is cached for 90% input token savings.',
      events: 'Subscribes to wildcard NATS JetStream subject TENANT_{id}.agent.*.output — the only agent with cross-agent read access. Aggregates: lead pipeline from Leads Agent, design status from Design Agent, schedule health from Project Management Agent, margin data from Sales Agent, and financial position from Bookkeeping Agent.',
      scheduling: 'BullMQ repeatable cron job fires at 7:00 AM daily per tenant\'s configured timezone. The briefing generation is a multi-step pipeline: (1) query each agent\'s latest outputs from Supabase, (2) pull escalations table for unresolved items, (3) generate narrative briefing via Claude, (4) store in drafts, (5) push WebSocket event to CEO dashboard. Total execution: ~30-60 seconds.',
      data: 'Briefings stored in the drafts table (CEO-only visibility enforced by RBAC — tenant_members role check). Escalation queue in the escalations table with priority sorting and age tracking. Rolling 12-month forecasts stored as JSONB in agent_executions. All outputs are CEO eyes only — employee roles (admin, manager, designer, bookkeeper) cannot access CEO Agent data.',
      hosting: 'Single multi-tenant VPS — all customers share infrastructure. This agent has the widest NATS subscription scope but the lowest invocation frequency (1x/day per tenant). Per-tenant Vercel AI Gateway key with spending limits. At scale: ~$0.15-0.50 per briefing depending on data volume and model tier used.',
    },
  },
  {
    id: 'support-agent',
    name: 'Support Agent',
    subtitle: 'Customer Support & Self-Service',
    color: '#ec4899',
    icon: 'MessageCircle',
    description: 'Handles customer questions with RAG-powered knowledge base and proactive support',
    details: [
      'Answers customer questions using a curated knowledge base with hybrid search (vector + keyword)',
      'Routes complex issues to humans with full context, sentiment analysis, and suggested resolution',
      'Proactively detects problems — failure streaks, expiring tokens, onboarding stalls, usage limits',
      'Tracks support metrics: CSAT, resolution rate, containment rate, and escalation patterns',
    ],
    integrations: ['email', 'jobtread', 'quickbooks'],
    humanApproval: [
      'All responses during 2-week shadow mode before go-live',
      'Escalation routing decisions for billing or account issues',
      'Knowledge base article publication and updates',
    ],
    metrics: [
      'Resolution rate (20-25% launch, 35-45% at 6 months)',
      'CSAT score (thumbs up/down)',
      'Containment rate (% resolved without human)',
      'Average response time',
    ],
    infrastructure: {
      execution: 'Node.js TypeScript process on the shared DigitalOcean VPS running Docker Compose. Implements the ToolLoopAgent pattern from Vercel AI SDK v6 with confidence-gated response routing. Temperature set to 0 for factual grounding. Tenant context injected at request boundary.',
      ai: 'Customer queries processed by Claude Haiku 4.5 ($1/1M input) for simple FAQ lookups. Complex troubleshooting escalates to Claude Sonnet 4.6 ($3/1M input) for multi-step reasoning. Knowledge base embeddings use text-embedding-3-small ($0.02/1M tokens). Hybrid retrieval combines pgvector cosine similarity with BM25 keyword search via Reciprocal Rank Fusion. Confidence >= 0.85 gives direct answer; 0.60-0.84 adds disclaimer; < 0.60 auto-escalates.',
      events: 'Subscribes to NATS JetStream subjects: TENANT_{id}.support.chat.message for incoming user messages and TENANT_{id}.support.proactive.* for proactive alerts. Chat widget sends messages via the Hono API at /api/support/chat/:tenantId, which validates the session, logs the event, and publishes to NATS.',
      scheduling: 'BullMQ proactive support worker runs 7 scheduled checks: failure streak detection (3+ in 24h), OAuth token expiry warnings (7-day advance), onboarding stall detection (no agent created after 48h), usage limit warnings (80% of daily cap), plus billing threshold, usage anomaly, and system health checks. Each fires a NATS event that triggers an in-app notification or email.',
      data: 'Knowledge base articles stored in support_articles table with pgvector embeddings in support_chunks (512-token chunks, 50-token overlap). Chat history in support_conversations with full message threading. All queries filtered by Supabase RLS (tenant_id = auth.jwt() ->> \'tenant_id\'). Platform-wide articles use tenant_id = NULL with public RLS policy. RAGAS evaluation scores tracked in support_evaluations table.',
      hosting: 'Single multi-tenant VPS — no per-customer servers. Tenant isolation via NATS subject namespacing (TENANT_{id}.support.*), Supabase RLS on all support tables, and per-tenant conversation history isolation. Support KB articles are shared across tenants (platform-wide) with tenant-specific customization layer. Adding a customer adds zero incremental support infrastructure cost.',
    },
  },
];
